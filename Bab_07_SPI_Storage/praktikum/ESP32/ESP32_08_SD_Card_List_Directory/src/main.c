#include <stdio.h>
#include "esp_system.h"
#include "config.h"

// Directory listing on SD card
// Key features: Dir listing, File enumeration, VFS scan

void app_main(void)
{
    printf("Starting 08_SD_Card_List_Directory\n");
    
    // TODO: Implement 08_SD_Card_List_Directory
    
    while (1) {
        vTaskDelay(100 / portTICK_PERIOD_MS);
    }
}
