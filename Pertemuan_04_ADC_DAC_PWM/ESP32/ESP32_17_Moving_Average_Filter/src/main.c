#include <stdio.h>
#include "esp_system.h"
#include "config.h"

// Moving average filter for signal smoothing
// Key features: Digital filter, IIR implementation, Noise removal

void app_main(void)
{
    printf("Starting 05_Moving_Average_Filter\n");
    
    // TODO: Implement 05_Moving_Average_Filter
    
    while (1) {
        vTaskDelay(100 / portTICK_PERIOD_MS);
    }
}
