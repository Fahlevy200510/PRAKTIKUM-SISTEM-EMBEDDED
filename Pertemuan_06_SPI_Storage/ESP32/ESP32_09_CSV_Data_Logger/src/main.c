#include <stdio.h>
#include "esp_system.h"
#include "config.h"

// CSV data logging to SD card
// Key features: CSV logging, Data format, Append mode

void app_main(void)
{
    printf("Starting 09_CSV_Data_Logger\n");
    
    // TODO: Implement 09_CSV_Data_Logger
    
    while (1) {
        vTaskDelay(100 / portTICK_PERIOD_MS);
    }
}
