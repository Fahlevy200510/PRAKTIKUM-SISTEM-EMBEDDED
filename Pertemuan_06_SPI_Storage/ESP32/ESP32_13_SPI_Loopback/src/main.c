#include <stdio.h>
#include "esp_system.h"
#include "config.h"

// SPI loopback test on ESP32
// Key features: SPI loopback, GPIO connection, Verification

void app_main(void)
{
    printf("Starting 01_SPI_Loopback\n");
    
    // TODO: Implement 01_SPI_Loopback
    
    while (1) {
        vTaskDelay(100 / portTICK_PERIOD_MS);
    }
}
